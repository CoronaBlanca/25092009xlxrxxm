local player = Players.LocalPlayer

local displayName = player.DisplayName
if not displayName or displayName == "" then
    displayName = player.Name
end

local title = ("Galaxy XVI Paid Version | Welcome %s"):format(displayName)

local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/CoronaBlanca/25092009UI/refs/heads/main/Xlxrxxm%20bx%20shxryx"))()

local window = library:AddWindow(title, {
    main_color = Color3.fromRGB(12, 8, 120),
    min_size = Vector2.new(600, 800),
    can_resize = true,
})

local TrollTab = window:AddTab("Troll")

TrollTab:AddLabel("Troll Scripts").TextSize = 24

TrollTab:AddLabel("-----{Black Hole}-----")

local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer

-- Variables globales
local angle = 1
local radius = 10
local blackHoleActive = false
local humanoidRootPart, Attachment1

--  Configuracin del jugador
local function setupPlayer()
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

    local Folder = Instance.new("Folder", Workspace)
    Folder.Name = "BlackHoleFolder"

    local Part = Instance.new("Part", Folder)
    local Attachment = Instance.new("Attachment", Part)
    Part.Anchored = true
    Part.CanCollide = false
    Part.Transparency = 1

    return humanoidRootPart, Attachment
end

humanoidRootPart, Attachment1 = setupPlayer()

--  Sistema de red local
if not getgenv().Network then
    getgenv().Network = {
        BaseParts = {},
        Velocity = Vector3.new(14.46262424, 14.46262424, 14.46262424)
    }

    Network.RetainPart = function(part)
        if typeof(part) == "Instance" and part:IsA("BasePart") and part:IsDescendantOf(Workspace) then
            table.insert(Network.BaseParts, part)
            part.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0)
            part.CanCollide = false
        end
    end

    local function EnablePartControl()
        LocalPlayer.ReplicationFocus = Workspace
        RunService.Heartbeat:Connect(function()
            sethiddenproperty(LocalPlayer, "SimulationRadius", math.huge)
            for _, part in pairs(Network.BaseParts) do
                if part:IsDescendantOf(Workspace) then
                    part.Velocity = Network.Velocity
                end
            end
        end)
    end

    EnablePartControl()
end

--  Funcion principal
local function ForcePart(v)
    if v:IsA("Part") and not v.Anchored and not v.Parent:FindFirstChild("Humanoid") and not v.Parent:FindFirstChild("Head") and v.Name ~= "Handle" then
        for _, x in next, v:GetChildren() do
            if x:IsA("BodyMover") or x:IsA("BodyVelocity") or x:IsA("BodyPosition") or x:IsA("BodyGyro") or x:IsA("Torque") then
                x:Destroy()
            end
        end
        if v:FindFirstChild("Attachment") then v.Attachment:Destroy() end
        if v:FindFirstChild("AlignPosition") then v.AlignPosition:Destroy() end

        v.CanCollide = false
        local Torque = Instance.new("Torque", v)
        Torque.Torque = Vector3.new(1000000, 1000000, 1000000)

        local AlignPosition = Instance.new("AlignPosition", v)
        local Attachment2 = Instance.new("Attachment", v)
        Torque.Attachment0 = Attachment2

        AlignPosition.MaxForce = math.huge
        AlignPosition.MaxVelocity = math.huge
        AlignPosition.Responsiveness = 500
        AlignPosition.Attachment0 = Attachment2
        AlignPosition.Attachment1 = Attachment1
    end
end

--  Alternador del agujero negro
local function toggleBlackHole()
    blackHoleActive = not blackHoleActive
    if blackHoleActive then
        for _, v in next, Workspace:GetDescendants() do
            ForcePart(v)
        end

        Workspace.DescendantAdded:Connect(function(v)
            if blackHoleActive then
                ForcePart(v)
            end
        end)

        spawn(function()
            while blackHoleActive and RunService.RenderStepped:Wait() do
                angle = angle + math.rad(2)
                local offsetX = math.cos(angle) * radius
                local offsetZ = math.sin(angle) * radius
                Attachment1.WorldCFrame = humanoidRootPart.CFrame * CFrame.new(offsetX, 0, offsetZ)
            end
        end)
    else
        Attachment1.WorldCFrame = CFrame.new(0, -1000, 0)
    end
end

--  Reasigna si el jugador respawnea
LocalPlayer.CharacterAdded:Connect(function()
    humanoidRootPart, Attachment1 = setupPlayer()
    if blackHoleActive then
        toggleBlackHole()
    end
end)

--  Elerium UI (TrollTab)
local blackHoleFolder = TrollTab:AddFolder("Black Hole")

-- Slider para el radio del agujero
blackHoleFolder:AddSlider({
    Name = "Black Hole Radius",
    Min = 1,
    Max = 100,
    Default = 10,
    Callback = function(value)
        radius = value
    end
})

-- Switch principal del efecto
blackHoleFolder:AddSwitch("Activate Black Hole", function(state)
    if state then
        if not blackHoleActive then
            toggleBlackHole()
        end
    else
        if blackHoleActive then
            toggleBlackHole()
        end
    end
end)

local targetPlayer = nil

local DescendantAddedConnection

--  Sistema base
local Folder = Instance.new("Folder", Workspace)
Folder.Name = "PullPartsFolder"

local Part = Instance.new("Part", Folder)
Part.Anchored = true
Part.CanCollide = false
Part.Transparency = 1

Attachment1 = Instance.new("Attachment", Part)

--  Red (mantiene la simulación activa)
if not getgenv().Network then
	getgenv().Network = {
		BaseParts = {},
		Velocity = Vector3.new(14.46262424, 14.46262424, 14.46262424)
	}

	Network.RetainPart = function(Part)
		if Part:IsA("BasePart") and Part:IsDescendantOf(Workspace) then
			table.insert(Network.BaseParts, Part)
			Part.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0)
			Part.CanCollide = false
		end
	end

	local function EnablePartControl()
		LocalPlayer.ReplicationFocus = Workspace
		RunService.Heartbeat:Connect(function()
			sethiddenproperty(LocalPlayer, "SimulationRadius", math.huge)
			for _, Part in pairs(Network.BaseParts) do
				if Part:IsDescendantOf(Workspace) then
					Part.Velocity = Network.Velocity
				end
			end
		end)
	end

	EnablePartControl()
end

-- Forzar partes físicas
local function ForcePart(v)
	if v:IsA("BasePart") and not v.Anchored and not v.Parent:FindFirstChildOfClass("Humanoid") and not v.Parent:FindFirstChild("Head") and v.Name ~= "Handle" then
		for _, x in ipairs(v:GetChildren()) do
			if x:IsA("BodyMover") or x:IsA("RocketPropulsion") or x:IsA("Torque") or x:IsA("AlignPosition") then
				x:Destroy()
			end
		end
		if v:FindFirstChild("Attachment") then v.Attachment:Destroy() end

		v.CanCollide = false
		local Torque = Instance.new("Torque", v)
		Torque.Torque = Vector3.new(100000, 100000, 100000)

		local AlignPosition = Instance.new("AlignPosition", v)
		local Attachment2 = Instance.new("Attachment", v)
		Torque.Attachment0 = Attachment2

		AlignPosition.MaxForce = math.huge
		AlignPosition.MaxVelocity = math.huge
		AlignPosition.Responsiveness = 200
		AlignPosition.Attachment0 = Attachment2
		AlignPosition.Attachment1 = Attachment1
	end
end

-- 🧠 Buscar jugador
local function getPlayer(name)
	local lowerName = string.lower(name)
	for _, p in pairs(Players:GetPlayers()) do
		if string.find(string.lower(p.Name), lowerName) or string.find(string.lower(p.DisplayName), lowerName) then
			return p
		end
	end
end

--  Activar / desactivar efecto
local function toggleBlackHole()
	blackHoleActive = not blackHoleActive
	if blackHoleActive and targetPlayer then
		local character = targetPlayer.Character or targetPlayer.CharacterAdded:Wait()
		humanoidRootPart = character:WaitForChild("HumanoidRootPart")

		for _, v in ipairs(Workspace:GetDescendants()) do
			ForcePart(v)
		end

		DescendantAddedConnection = Workspace.DescendantAdded:Connect(function(v)
			if blackHoleActive then
				ForcePart(v)
			end
		end)

		spawn(function()
			while blackHoleActive and RunService.RenderStepped:Wait() do
				if humanoidRootPart then
					Attachment1.WorldCFrame = humanoidRootPart.CFrame
				end
			end
		end)
	else
		if DescendantAddedConnection then
			DescendantAddedConnection:Disconnect()
		end
	end
end

--  UI en Elerium v2
local pullFolder = TrollTab:AddFolder("Pull Parts")

--  Caja de texto para seleccionar jugador
pullFolder:AddBox({
	Name = "Player Name",
	Default = "",
	Placeholder = "Enter player name...",
	TextDisappear = false,
	Callback = function(value)
		local found = getPlayer(value)
		if found then
			targetPlayer = found
			print(" Player selected:", found.Name)
		else
			targetPlayer = nil
			print(" Player not found.")
		end
	end
})

--  Switch principal
pullFolder:AddSwitch("Pull Parts", function(state)
	if targetPlayer then
		if state then
			if not blackHoleActive then
				toggleBlackHole()
			end
		else
			if blackHoleActive then
				toggleBlackHole()
			end
		end
	else
		print("No player selected.")
	end
end)

-- 🧩 Switch en TrollTab
TrollTab:AddSwitch("Become Invisible", function(state)
    if state then
        -- 🕶️ Activar invisibilidad FE
        local args = {
            [1] = {
                [1] = 102344834840946,
                [2] = 70400527171038,
                [3] = 0,
                [4] = 0,
                [5] = 0,
                [6] = 0
            }
        }

        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local Remotes = ReplicatedStorage:WaitForChild("Remotes")

        -- Cambiar cuerpo e invisibilizar
        Remotes:WaitForChild("ChangeCharacterBody"):InvokeServer(unpack(args))
        Remotes:WaitForChild("Wear"):InvokeServer(111858803548721)

        local allaccessories = {}
        local humanoid = game.Players.LocalPlayer.Character:WaitForChild("Humanoid")
        local desc = humanoid and humanoid:FindFirstChildOfClass("HumanoidDescription")

        if desc then
            for _, accessoryType in ipairs({
                desc.BackAccessory,
                desc.FaceAccessory,
                desc.FrontAccessory,
                desc.NeckAccessory,
                desc.HatAccessory,
                desc.HairAccessory,
                desc.ShouldersAccessory,
                desc.WaistAccessory,
                desc.GraphicTShirt
            }) do
                for id in string.gmatch(accessoryType, "%d+") do
                    table.insert(allaccessories, tonumber(id))
                end
            end
        end

        task.wait()

        for _, accessoryId in ipairs(allaccessories) do
            task.spawn(function()
                Remotes:WaitForChild("Wear"):InvokeServer(accessoryId)
                print("[Invisible FE] Accessory reapplied:", accessoryId)
            end)
        end
    else
        --  Si quieres que al apagar el switch reaparezca visible, puedes reiniciar personaje:
        local lp = game.Players.LocalPlayer
        lp.Character:BreakJoints()
        print("[Invisible FE] Visibility reset.")
    end
end)
